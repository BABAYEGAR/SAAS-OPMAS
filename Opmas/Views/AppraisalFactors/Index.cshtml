@using Opmas.Data.Service.Enums
@model IEnumerable<Opmas.Data.Objects.PerformanceManagement.AppraisalFactor>
@{
    ViewBag.Title = "List";
    ViewBag.Header = "Appraisal Factor";
    ViewBag.Subheader = "List Of Appraisal Factors";
    Layout = "~/Views/Shared/Layout/_LayoutListItems.cshtml";
}
@if (TempData["appraisalfactor"] != null)
{
    if (TempData["notificationtype"].Equals(NotificationTypeEnum.Success.ToString()))
    {
        <div class="alert alert-success alert-dismissable">@TempData["appraisalfactor"]</div>
    }
    else if (TempData["notificationtype"].Equals(NotificationTypeEnum.Error.ToString()))
    {
        <div class="alert alert-danger alert-dismissable">@TempData["appraisalfactor"]</div>
    }
    else if (TempData["notificationtype"].Equals(NotificationTypeEnum.Info.ToString()))
    {
        <div class="alert alert-info alert-dismissable">@TempData["appraisalfactor"]</div>
    }
}
@section add {
    @Html.ActionLink("Add data", "Create", null, new { @class = "btn btn-primary btn-xs fa fa-plus" })
}
<table id="datatable" class="table table-striped table-bordered">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AppraisalCategory.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Factor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FactorScore)
        </th>
        <th>Action</th>
    </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AppraisalCategory.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Factor)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FactorScore)
                </td>
                <td>
                    @Html.ActionLink("Details", "Details", new {id = item.AppraisalFactorId}, new {@class = "btn btn-xs btn-default fa fa-television"})
                </td>
            </tr>
        }
    </tbody>
</table>