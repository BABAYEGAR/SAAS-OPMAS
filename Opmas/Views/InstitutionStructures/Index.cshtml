@model IEnumerable<Opmas.Data.Objects.Entities.SystemManagement.InstitutionStructure>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Institution.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Faculty)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Institution.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Faculty)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.InstitutionStructureId }) |
                @Html.ActionLink("Details", "Details", new { id=item.InstitutionStructureId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.InstitutionStructureId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
