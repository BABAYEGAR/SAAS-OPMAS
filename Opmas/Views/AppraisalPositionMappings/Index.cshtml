@model IEnumerable<Opmas.Data.Objects.Mappings.AppraisalPositionMapping>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AppraisalCategory.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmploymentPosition.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedBy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateCreated)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateLastModified)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastModifiedBy)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AppraisalCategory.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmploymentPosition.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedBy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateCreated)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateLastModified)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastModifiedBy)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.AppraisalPositionMappingId }) |
                @Html.ActionLink("Details", "Details", new { id=item.AppraisalPositionMappingId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.AppraisalPositionMappingId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
